#include <stdio.h>

int main()
{
    float a = 0.1;
    float b = 0.2;
    float suma = a + b;

    if (suma = 0.3)
    {
        printf("Suma jest rowna 0.3\n");
    }
    else
    {
        printf("Suma nie jest rowna 0.3, suma to: %.1f\n", suma);
    }

    return 0;
}